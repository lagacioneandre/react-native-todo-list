import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyList: {
    alignItems: 'center',
  },
  fileIcon: {
    width: 56,
    height: 56,
    marginBottom: 15,
  },
  emptyTitle: {
    fontWeight: 'bold',
    fontSize: 14,
    color: '#808080',
  },
  emptyText: {
    fontSize: 14,
    color: '#808080',
  },
});
